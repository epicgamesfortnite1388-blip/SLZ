"""Core platform primitives shared by every module.

Contains no business logic. Provides base models, standardized errors, the API
error envelope, pagination, the in-process domain-event bus, correlation-id
middleware, health probes and reusable versioning abstractions.
"""
